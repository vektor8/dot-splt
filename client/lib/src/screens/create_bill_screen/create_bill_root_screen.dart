import 'package:flutter/material.dart';

class CreateBillRootScreen extends StatelessWidget {
  const CreateBillRootScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Create'),
      ),
    );
  }
}
